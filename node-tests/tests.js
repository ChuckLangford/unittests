'use strict';

// ----------------------------------------------------------------------------
// The idea behind the tests.js file is to simply run each of the individual
// module tests that are in the same directory.  Each module unit test needs to
// export a single function.  That function simply runs all the asserts.
// ----------------------------------------------------------------------------
require('./test-module')();
